#spring:
#  main:
#    allow-bean-definition-overriding: true
    
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://auth.dev.witcom.services/auth/realms/witcom

logging:
  level: 
    org.springframework: WARN


management:
  endpoints:
    web:
      exposure:
        include: '*'